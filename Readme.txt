DayPlanner
A simple calendar application that allows the user to save events for each hour of the day.

With the use of boostrap, html, css,js and jquery to input click events.

Local storage is used to store appointments on click